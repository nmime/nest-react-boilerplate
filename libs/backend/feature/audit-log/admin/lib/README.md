# @app/backend-feature-audit-log-admin

## Purpose

Owns read-only audit log administration endpoints, filters, metadata, and tenant-scoped inspection.

## Commands

```bash
pnpm exec nx run @app/backend-feature-audit-log-admin:test
pnpm exec nx run @app/backend-feature-audit-log-admin:build
```
