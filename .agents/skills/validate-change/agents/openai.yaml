interface:
  display_name: 'Validate Change'
  short_description: 'Select and run proportional change validation'
  default_prompt: 'Use $validate-change to validate this repository change proportionally.'
