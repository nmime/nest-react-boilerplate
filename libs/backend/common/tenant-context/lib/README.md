# @app/backend-common-tenant-context

## Purpose

Ambient tenant scope: CLS tenant propagation, a fail-closed HTTP interceptor, and the exemption decorator that lets a route declare it legitimately has no tenant.

## Commands

```bash
pnpm exec nx run @app/backend-common-tenant-context:test
pnpm exec nx run @app/backend-common-tenant-context:build
```
