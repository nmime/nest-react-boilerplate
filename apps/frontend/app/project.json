{
  "name": "user-app",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend/app/src",
  "projectType": "application",
  "tags": ["platform:frontend", "type:frontend-app", "scope:user", "type:app"],
  "// targets": "to see all targets run: nx show project user-app --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "cwd": "apps/frontend/app",
        "command": "vitest run --config vitest.config.mts"
      },
      "inputs": [
        "default",
        "^production",
        {
          "externalDependencies": ["vitest"]
        }
      ],
      "outputs": ["{workspaceRoot}/coverage/apps/frontend/app"]
    },
    "e2e": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "options": {
        "command": "node tools/frontend-static-smoke.mjs --dist dist/apps/frontend/app --app-name user-app --contains \"Personal crypto operations in one reliable workspace.\""
      },
      "inputs": [
        "production",
        {
          "externalDependencies": []
        }
      ],
      "outputs": []
    }
  }
}
