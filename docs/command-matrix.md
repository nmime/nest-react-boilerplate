# Command matrix

Use this matrix as the supported DX contract for local development and CI. Prefer these package scripts over direct `nx` or ad-hoc direct implementation calls so command names stay stable while implementations move.

| Goal                         | Command                                                                                                                                                                                 | When to run                               | Notes                                                                                                       |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Start selected development  | `pnpm dev`                                                                                                                                                                              | Daily cross-app development               | Requires setup and starts explicit serve projects from `.nrb/closure.json`.                                 |
| Start all serve targets     | `pnpm dev:all`                                                                                                                                                                          | Explicit full workspace walkthroughs      | Uses Nx `run-many -t serve`; all supported applications are included intentionally.                         |
| Start local dev selection   | `pnpm dev:fullstack`                                                                                                                                                                    | Selected cross-app checks                 | Starts explicit serve projects from `.nrb/closure.json`; refuses to choose applications before setup.       |
| Start mobile app             | `pnpm dev:mobile`                                                                                                                                                                       | Mobile development                        | Starts the Expo dev server for `mobile-app` on `localhost:4300`; use the Expo UI/QR flow for iOS, Android, or web. |
| Run mobile native targets    | `pnpm run mobile:android` / `pnpm run mobile:ios`                                                                                                                                       | Native simulator/device checks            | Runs Expo native build targets for Android or iOS. Requires the matching local native toolchain.            |
| Run mobile web/export        | `pnpm run mobile:web` / `pnpm run mobile:export`                                                                                                                                        | Mobile web smoke or static export         | Starts the Expo web dev server on `localhost:4300` or exports the web bundle to `dist/apps/frontend/mobile`. |
| Start selected local database | `pnpm dev:db`                                                                                                                                                                           | Before local database work                 | Reads `.nrb` setup output and starts only PostgreSQL or MongoDB plus its replica-set initializer; rejects missing or conflicting providers. |
| Build selected projects      | `pnpm build`                                                                                                                                                                            | Daily product builds                      | Runs `build` only for explicit projects recorded in `.nrb/closure.json`.                                    |
| Build every project          | `pnpm run build:all`                                                                                                                                                                    | Explicit maintainer workspace sweeps      | Runs every Nx build target; never used as a selected-project fallback.                                      |
| Check selected closure       | `pnpm nrb closure check`                                                                                                                                                                | After setup or dependency/source changes  | Compares generated closure/package manifests with the selected live Nx graph without installing.            |
| Install selected closure     | `pnpm nrb closure install`                                                                                                                                                              | When a scoped local dependency tree is wanted | Installs the clean selected dependency tree and writes the normalized `.nrb/closure` named BuildKit context; no default-context fallback exists. |
| Install maintainer tooling   | `pnpm run tooling:install`                                                                                                                                                              | Before repository-wide maintenance or setup bootstrap | Explicitly removes selected links and restores the full root pnpm workspace from `pnpm-lock.yaml`. |
| Materialize all-reference closure | `pnpm nrb closure materialize --all-reference --provider <postgres\|mongodb>`                                                                                                      | Explicit maintainer validation             | Writes a complete isolated config/package/workspace/lock context under `.nrb/reference/<provider>`; never acts as a product fallback. |
| Generate selected Bake plan  | `pnpm run bake:generate`                                                                                                                                                                | Before a selected product image build      | Fails closed unless the selected closure and normalized context are current; every target uses `nrb-closure=.nrb/closure`. |
| Generate all-reference Bake plan | `pnpm run bake:generate:all`                                                                                                                                                            | Explicit maintainer image validation       | Materializes PostgreSQL and attaches `.nrb/reference/postgres` as the `nrb-closure` BuildKit context; product release workflows never use this mode. |
| Verify Bun compatibility     | `pnpm run bun:check`                                                                                                                                                                   | Bun runtime or cross-runtime changes       | Requires `closure install`; every selected server artifact must pass a real isolated Node and pinned-Bun startup/readiness/lifecycle or headless process probe. Durable selections require Docker. |
| Map workspace dependencies   | `pnpm run deps:map`                                                                                                                                                                    | Dependency review and ownership discovery  | Reports live workspace/scope counts; add `-- --json` for machine-readable package names and dependency lists. |
| Selected unit tests          | `pnpm test`                                                                                                                                                                             | Before every product PR                   | Runs `test` for every testable project in `.nrb/closure.json`, including transitive libraries.              |
| Selected coverage gate       | `pnpm run test:coverage`                                                                                                                                                                | Product runtime TypeScript changes         | Runs selected closure test targets with coverage.                                                           |
| Selected component tests     | `pnpm test:component`                                                                                                                                                                   | Product component changes                 | Runs every selected closure project with a `component-test` target.                                         |
| Selected E2E smoke           | `pnpm test:e2e`                                                                                                                                                                         | Product cross-app behavior changes         | Runs only selected closure projects with an `e2e` target.                                                   |
| All-reference test sweeps    | `pnpm run test:all` / `test:coverage:all` / `test:component:all` / `test:e2e:all` / `test:e2e:coverage:all`                                                                            | Explicit maintainer and template CI        | Runs the repository-wide lanes without inventing a product selection.                                       |
| Docker fullstack             | `pnpm run docker:fullstack`                                                                                                                                                             | Docker/local stack validation             | Builds and starts the setup-selected Compose stack through the validated `.nrb/closure` named context.      |
| Docker smoke                 | `pnpm run test:docker-smoke`                                                                                                                                                            | Docker image or Compose changes           | Runs the Docker smoke stack validation used by CI.                                                          |
| Deployment config validation | `node scripts/validate-deployment-config.mjs`                                                                                                                                           | Docker, Helm, env, or deployment changes  | Runs static deployment assertions for the selected mode; CI runs it before strict Helm rendering.           |
| All-reference Docker validation | `pnpm run deploy:validate:docker:all-reference`                                                                                                                                        | Explicit template maintainer validation   | Materializes and verifies the complete PostgreSQL context, then exposes its root/manifest to the Docker validation suite. |
| Helm rate-limit validation   | `node scripts/validate-helm-rate-limit-config.mjs`                                                                                                                                      | Helm or API rate-limit/Redis (`@redis/client`) changes      | Runs focused Helm Redis/rate-limit assertions; CI runs it before Helm rendering.                            |
| Docker teardown              | `pnpm run docker:down`                                                                                                                                                                  | After Docker validation                   | Stops the full-stack Compose services and removes orphans.                                                  |
| Fullstack Playwright         | `pnpm run test:fullstack`                                                                                                                                                               | Docker-backed full-stack behavior         | Requires `fullstack-e2e` in a fresh selected closure and derives every app, capability service, and provider from it. |
| Selected lint               | `pnpm lint`                                                                                                                                                                             | Before product PRs                        | Lints every selected root/transitive project with a lint target.                                            |
| All-project lint            | `pnpm run lint:all`                                                                                                                                                                     | Explicit maintainer/CI sweeps             | Lints all Nx projects without inventing a product selection.                                                |
| Selected typecheck          | `pnpm typecheck`                                                                                                                                                                        | Before product PRs                        | Typechecks every selected root/transitive project with a typecheck target.                                  |
| All-project typecheck       | `pnpm run typecheck:all`                                                                                                                                                                | Explicit maintainer/CI sweeps             | Typechecks all Nx projects without inventing a product selection.                                           |
| Format                       | `pnpm format` / `pnpm format:check`                                                                                                                                                     | Before PR / CI                            | Prettier with unknown file support.                                                                         |
| Fast PR preflight            | `pnpm run check:fast`                                                                                                                                                                   | Before every PR                           | Runs the tooling static check and docs check, then format check plus Nx lint, typecheck, and tests without release-risk gates. |
| Dependency audit             | `pnpm run audit`                                                                                                                                                                        | Before dependency PRs and CI              | Runs the repository script with moderate-or-higher vulnerability gating.                                    |
| Database migrate             | `pnpm db:migrate`                                                                                                                                                                       | After changing migrations                 | Local commands require the fresh selected closure provider and reject provider-free or mismatched environment selectors; final migrator images instead require matching explicit deployment selectors without `.nrb`. |
| Migration standards check    | `pnpm db:migrations:check`                                                                                                                                                              | Before PR with DB changes                 | Scans all migration files; enforces SQL naming rules, while MongoDB exact drift is proved by migration/component tests. |
| API OpenAPI export           | `pnpm api:openapi`                                                                                                                                                                      | API shape changes                         | Produces OpenAPI contracts.                                                                                 |
| API client generation        | `pnpm api:clients`                                                                                                                                                                      | After OpenAPI changes                     | Updates generated clients.                                                                                  |
| API contract check           | `pnpm api:contracts:check`                                                                                                                                                              | CI and API PRs                            | Fails on stale contracts.                                                                                   |
| API client drift check       | `pnpm api:clients:check`                                                                                                                                                                | CI and API PRs                            | Fails on stale generated clients.                                                                           |
| API OpenAPI lint             | `pnpm api:openapi:lint`                                                                                                                                                                 | API shape or contract changes             | Runs the OpenAPI linter preset.                                                                             |
| API OpenAPI fuzz             | `pnpm api:openapi:fuzz`                                                                                                                                                                 | Release-risk API changes                  | Exercises generated contracts with the fuzzing preset.                                                      |
| Consumer contract checks     | `pnpm api:contracts:consumer`                                                                                                                                                           | Contract/provider changes                 | Runs consumer contract assertions.                                                                          |
| Database seed/reset          | `pnpm db:seed` / `pnpm db:reset`                                                                                                                                                        | Local data setup                          | Uses the fresh selected durable provider, loads only its command graph, fails provider-free, and retains local safety guards. |
| Database backup/restore      | `pnpm db:backup` / `pnpm db:restore`                                                                                                                                                    | Backup or restore validation              | Uses the fresh selected closure provider, loads only its archive client, and redacts connection details.    |
| Restore drill                | `pnpm db:restore:drill` / `pnpm test:restore-drill`                                                                                                                                     | DR or backup changes                      | Validates the fresh selected closure before dispatching the real or CI-safe drill to one provider.          |
| Migration rollback check     | `pnpm test:migrations:rollback`                                                                                                                                                         | PostgreSQL migration changes              | Runs PostgreSQL up/down/up; MongoDB instead proves idempotent apply/verify in component tests.              |
| Storybook dev/build | `pnpm storybook` / `pnpm run storybook:build` | UI documentation and review | Uses `libs/frontend/ui-web/lib/.storybook` for shared web UI plus explicit admin, user, landing, and site screen compositions; builds to `dist/storybook/frontend-ui-web`. |
| UI registry discovery/import | `pnpm run ui:registry:search -- --source <source>` / `pnpm run ui:registry:add -- --source <source> <item>` / `pnpm run ui:shadcn:add -- <component>` | Shared web UI primitive or reviewed creative effect | Uses the pinned shadcn registry client in the `libs/frontend` package scope. Shadcn and free Magic UI are dry-run-first, source-owned inputs. Aceternity is non-persistent research preview only and is never distributed by the template; a downstream product must explicitly own any later integration. Paid, authenticated, direct-URL, and path-override inputs are rejected. |
| Storybook test | `pnpm run test:storybook` | UI behavior/accessibility checks | Runs shared component and deterministic web app-composition stories in Chromium; app routing, auth, providers, APIs, and full flows remain in e2e. |
| Visual regression | `pnpm run test:visual` / `pnpm run test:visual:matrix` / `pnpm run test:visual:update` | Rendered UI changes | Checks reviewed, platform-and-browser-specific full-page Storybook baselines. The matrix adds Firefox, WebKit, and mobile profiles; update commands require manual PNG review. |
| E2E coverage                 | `pnpm run test:e2e:coverage`                                                                                                                                                            | Cross-app coverage changes                | Runs e2e targets with coverage enabled.                                                                     |
| Browser/device matrix        | `pnpm run test:e2e:matrix`                                                                                                                                                              | Cross-browser/mobile risk                 | Runs the extended Playwright matrix preset.                                                                 |
| Accessibility                | `pnpm run test:a11y`                                                                                                                                                                    | UI and route changes                      | Runs the accessibility preset.                                                                              |
| Performance/load             | `pnpm run test:perf` / `pnpm run test:load`                                                                                                                                             | Performance-sensitive changes             | Runs local performance or load presets.                                                                     |
| Security checks              | `pnpm run test:security`                                                                                                                                                                | Security-sensitive changes                | Runs the bundled SAST, secret-scan, and DAST suite; PRs separately gate native SAST and secret scan.        |
| Security focused checks      | `pnpm run test:security:sast`, `pnpm run test:security:secrets`, `pnpm run test:security:dast`                                                                                          | Targeted security work                    | Run individual security presets; native SAST and secret scanning are hard PR gates, DAST remains runtime-scoped. |
| Mutation/property tests      | `pnpm run test:mutation` / `pnpm run test:property`                                                                                                                                     | Logic-heavy changes                       | Run mutation or property-based QA presets.                                                                  |
| Reliability/ops drills       | `pnpm run test:chaos`, `pnpm run test:disaster-recovery`, `pnpm run test:backup-restore`, `pnpm run test:observability`, `pnpm run test:concurrency`, `pnpm run test:reliability:smoke` | Operations-sensitive changes              | Run focused reliability and observability presets.                                                          |
| OpenTelemetry tests          | `pnpm run test:otel`                                                                                                                                                                    | Telemetry instrumentation changes         | Runs telemetry-specific tests.                                                                              |
| World-class QA gate          | `pnpm run test:world-class` / `pnpm run quality:world-class`                                                                                                                            | Release-risk or scheduled QA sweeps       | Runs the high-signal QA gate; local missing-runtime gates report partial, CI missing-runtime gates fail unless explicitly allowed. |
| Tooling static check | `pnpm run tooling:static-check` | Tooling/CI PRs | Syntax-checks tooling modules, typechecks `@repo/tooling`, import-smokes safe CLIs, verifies package script references, and runs FSD self/workspace checks. |
| Documentation check | `pnpm run docs:check` | Documentation, commands, or structure changes | Checks generated project-catalog parity, documentation-index reachability, duplicate project metadata, local targets, heading anchors, and root `pnpm run` script references. |
| Frontend FSD check | `pnpm run frontend:fsd:check` | Frontend architecture PRs | Enforces FSD layer/slice boundaries, frontend project tags, and public APIs. |
| Library config check | `pnpm run lib:configs:check` | Library split/config changes | Requires library config and library Storybook config files to live inside each library `lib/` folder and rejects legacy auth OAuth placement. |
| Project initialization       | `pnpm nrb init --name <title> --domain <base> --owner <org>`                                                                                                                            | New product setup                         | Replaces product identity and every example frontend/API domain; dry-run first.                             |
| Select/update applications   | `pnpm nrb setup` / `pnpm nrb setup --app <id> --non-interactive`                                                                                                                        | First setup or adding an app later        | Interactive custom selection; scripted app/capability flags add to the existing selection.                 |
| Agent scaffold acceptance    | `pnpm run agent:verify`                                                                                                                                                                 | Setup, generators, or agent guidance      | Proves repeatable selection, explicit ownership, clone/force refusal, and canonical instruction routing.    |
| Agent skill package check    | `pnpm run agent:skills:check`                                                                                                                                                           | Repo-local skill changes                  | Tests the validator, then checks names, trigger descriptions, required context/evidence sections, local references, interface metadata, prompts, package hygiene, catalog entries, and workflow discovery without network access. |
| Inspect application choices  | `pnpm nrb setup --list`                                                                                                                                                                 | Before setup changes                      | Shows frontend, backend, E2E, and capability entries with current selection markers.                       |
| Verify fresh onboarding      | `pnpm run onboarding:verify`                                                                                                                                                            | Fresh clone and scaffold changes          | Runs doctor, all five preset dry-runs, and real build/test verification for six app plus three library templates. |
| Helm validation              | `pnpm run helm:validate`                                                                                                                                                                | Helm chart changes                        | Requires setup-generated `.helm/values-selection.yaml`; no all-app fallback exists.                        |
| Generate a vertical slice    | `pnpm generate:feature <name> -- --api-app <api> --frontend-app <app> --database <postgres|mongodb> --dry-run`                                                                         | Before starting a product feature         | Derives the selected provider when omitted and rejects workspace/provider mismatches.                      |
| Verify app/library scaffolds | `pnpm run scaffold:verify`                                                                                                                                                              | Generator or dependency changes           | Generates six app and three library canaries, builds/tests all nine, runs applicable typechecks, and removes them. |
| Full quality gate            | `pnpm check`                                                                                                                                                                            | Before merging release-risk work          | Runs formatting, API, QA, lint, typecheck, and tests.                                                       |
| Quality preset sweep         | `pnpm run quality:presets`                                                                                                                                                              | Release-risk or scheduled QA sweeps       | Runs the modern QA preset bundle documented in `docs/testing/modern-qa.md`.                                 |
| Visual quality matrix | `pnpm run quality:visual` | Scheduled or release-risk web UI sweeps | Builds Storybook, runs its interaction suite, then checks all reviewed desktop/mobile browser baselines. |

## Public root script details

These public root scripts are part of the supported DX/CI contract even when the primary table groups them under a broader goal:

| Script | Actual command | Notes |
| ------ | -------------- | ----- |
| `pnpm run format:changed` | `pnpm --filter @repo/tooling tooling tooling changed-format-check` | Runs the tooling changed-file Prettier check against `origin/main...HEAD` by default; pass `--base`/`--head` through the tooling command for another range. |
| `pnpm run docs:catalog` | `node scripts/generate-project-catalog.mjs` | Regenerates the canonical app catalog from setup metadata and Nx `project.json` roots. |
| `pnpm run docs:check` | catalog parity check + documentation validator tests + workspace Markdown validation | Rejects a stale project catalog, unindexed docs, copied leaf-project metadata, duplicate project-map sections, missing local targets/anchors, and unknown root scripts in docs or repo-local skills without network access. |
| `pnpm run bun:check` | `bun run --bun packages/tooling/bin/repo-tooling.mjs tooling bun-compat` | Runs the setup-selected Node/Bun artifact contract without replacing pnpm dependency resolution, lockfiles, or Node coverage gates. |
| `pnpm run images:webp` | `pnpm --filter @repo/tooling tooling images webp` | Converts PNG/JPG/JPEG assets to WebP side-by-side by default; pass inputs and options after `--`. |
| `pnpm run images:webp:check` | `pnpm --filter @repo/tooling tooling images webp --dry-run` | Non-mutating image conversion preview for local and PR checks. |
| `pnpm run ui:shadcn:add -- <component>` | `pnpm --filter @repo/tooling tooling ui shadcn add <component>` | Previews an official shadcn component import into `libs/frontend/ui-web/lib/src/component`; pass `--apply` to write and `--overwrite` only with `--apply`. |
| `pnpm run ui:registry:search -- --source <source>` | `pnpm --filter @repo/tooling tooling ui registry search --source <source>` | Searches the explicit `shadcn`, `magicui`, or `aceternity` namespace without writing source. |
| `pnpm run ui:registry:add -- --source <source> <item>` | `pnpm --filter @repo/tooling tooling ui registry add --source <source> <item>` | Previews source by default. Shadcn/Magic UI apply reruns a source-visible, path-confined preflight. Aceternity preview is temporary research only; the template cannot write or distribute it, and each downstream product must make an explicit product-local integration decision. |
| `pnpm run docker:prod:config:check` | `node scripts/validate-docker-compose-prod.mjs` | Static production Compose config validation only; it does not start containers. |
| `pnpm run docker:prod:config` | `node scripts/compose-production.mjs config` | Renders the database + domain + TLS + optional-profile topology selected in `.env.production`. |
| `pnpm run docker:prod:up` | `node scripts/compose-production.mjs up -d` | Starts the validated production topology; the Compose edge is included only for `single-domain` or `per-app-domains`. |
| `pnpm run docker:prod:down` | `node scripts/compose-production.mjs down` | Stops the same selected topology without deleting volumes. |
| `pnpm run deploy:validate` | `node scripts/deploy-validate.mjs` | No-deploy validation bundle. Renders both Compose database topologies when Docker Compose is available, validates Argo CD and Flux, and renders Helm when available. CI can require every CLI with `REQUIRE_*` flags. |
| `pnpm run deploy:validate:docker` | `node scripts/deploy-validate.mjs --mode=docker` | No-deploy validation of database, one-domain, per-app/wildcard-DNS, external-proxy, TLS, and optional-profile Compose contracts. |
| `pnpm run test:single-server-deployment` | `node --test scripts/single-server-deployment.spec.mjs` | Tests exact hosts, one-domain/per-app Nginx, same-origin routes, TLS, profiles, and wildcard SANs. |
| `pnpm run server:validate` | `node scripts/validate-single-server-deployment.mjs` | Static no-root validation of the bootstrap/controller, runtime pins, examples, renderer, and runbook. |
| `pnpm run server:render-nginx -- --server-env <path> --production-env <path>` | single-server renderer | Renders host Nginx for review; the root controller installs it atomically after `nginx -t`. |
| `pnpm run docker:prod:bundled-db:config` | merged production base + selected bundled-db overlay | Renders a Compose-owned PostgreSQL service or one-node MongoDB replica set according to `DATABASE_ENGINE`. |
| `pnpm run docker:prod:external-db:config` | merged production base + selected external-db overlay | Renders no database service and mounts `DATABASE_URL`, or separate MongoDB runtime and migration URI secrets; backup/restore tooling reads its dedicated URI file directly. |
| `pnpm run deploy:validate:pm2` | `node scripts/deploy-validate.mjs --mode=pm2` | No-deploy PM2 validation. If no `ecosystem.config.{js,cjs,mjs}` exists, this optional mode reports a skip/no-op and exits successfully. |
| `pnpm run deploy:validate:gitops` | `node scripts/deploy-validate.mjs --mode=gitops` | Strict Helm plus Argo CD and Flux manifest/Kustomize validation; controller installation and sync are platform responsibilities. |
| `pnpm run deploy:validate:helm` | `node scripts/deploy-validate.mjs --mode=helm` | Strict Helm 4 lint/render validation for direct Kubernetes or GitOps releases. |
| `pnpm run deploy:validate:helm:all-reference` | explicit PostgreSQL all-reference materialization plus strict Helm validation | Template-maintainer chart sweep; never used as a product-selection fallback. |
| `pnpm run security:scan` | `pnpm run test:security:secrets && pnpm run test:security:sast` | Runs the repository secret scan and SAST scripts in sequence. |
| `pnpm run ci:pr` | `pnpm run tooling:static-check && pnpm run docs:check && pnpm run format:changed && pnpm run test:security:secrets && pnpm run test:security:sast && pnpm run audit:ci` | Lightweight PR gate for static tooling, documentation contracts, changed formatting, security scans, and production dependency audit. |
| `pnpm run ci:workflows:check` | `node scripts/validate-github-workflows.mjs` | Static GitHub Actions workflow validation. |
| `pnpm run scaffold:verify` | `node packages/tooling/scripts/verify-generated-scaffolding.mjs` | Real generate/build/test verification for Vite, Astro, Vike, Expo, Nest API, consumer, scheduler, and backend/frontend/common library scaffolds. |
| `pnpm run onboarding:verify` | `node scripts/validate-onboarding.mjs && pnpm run agent:verify && pnpm run scaffold:verify` | Non-deploying fresh-install proof for runtime prerequisites, all five required/optional preset closures, and all app/library templates. |
| `pnpm run branch:cleanup` | `pnpm --filter @repo/tooling tooling git branch-cleanup` | Branch cleanup implementation entrypoint; use explicit flags such as `--apply` and `--remote` for destructive cleanup. |
| `pnpm run branch:cleanup:check` | `pnpm --filter @repo/tooling tooling git branch-cleanup --target HEAD` | Safe preview of branches already merged into `HEAD`. |
| `pnpm run git:conventions` | `pnpm --filter @repo/tooling tooling git conventions` | Validate naming, Conventional Commits, linear history, and agent attribution without rejecting legitimate human or dependency-bot identities. CI supplies an explicit push or PR range. |
| `pnpm run db:migrations:rollback-check` | `pnpm --filter @repo/tooling tooling db migrations rollback-check` | Real Testcontainers/PostgreSQL migration up/down/up validation; MongoDB has no down-migration equivalent. |

Formatting uses Prettier defaults intentionally: the repository has `.prettierignore` but no explicit Prettier config, so `pnpm run format`, `pnpm run format:check`, and `pnpm run format:changed` should be interpreted as stock Prettier behavior with `--ignore-unknown`.

Duplicate public aliases are retained for compatibility: `audit:ci` and `audit:lockfile` both run `pnpm audit --audit-level=moderate --prod`; `test:backup-restore` and `test:restore-drill` both run the CI dry-run restore drill. Prefer the descriptive names in new docs, but do not remove the aliases without checking workflow and documentation references.

`pnpm run tooling:static-check` also enforces stale architecture/version wording guards. It blocks retired exception aliases/paths, project-owned problem-wrapper names, stale package-manager/current-Node references, and obsolete Problem Details RFC references while allowing generic Problem Details language and RFC9457 wording.

## Recommended PR preflight

For most changes, run the fast local preflight before opening a PR:

```bash
pnpm run check:fast
```

Add targeted checks from the table above for migrations, dependency changes, cross-app behavior, Docker/deployment work, or release-risk changes.

## Project target discovery

Use the [Project Catalog](project-catalog.md) for selectable application IDs and
roots. For applications and libraries, inspect the live Nx contract instead of
maintaining another command table:

```bash
pnpm exec nx show projects
pnpm exec nx show project <id>
```

The nearest project README lists its high-signal verification commands.

## Tooling policy

- Add new local automation under `packages/tooling/src` and expose it through `packages/tooling/bin/repo-tooling.mjs` plus a root package script when it is part of the public DX.
- Do not add a root-level `tools/` directory or compatibility wrappers under
  `scripts/`. The existing `scripts/` directory owns root validators and
  deployment/orchestration entrypoints named by `package.json`; new reusable
  repository automation belongs in the workspace tooling package.
- Keep public commands on the root `package.json` scripts; direct `packages/tooling/bin/repo-tooling.mjs ...` calls are implementation details unless this matrix documents otherwise.
- If a command appears in docs, it must appear here or in `packages/tooling/README.md`.
