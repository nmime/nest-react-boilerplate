node_modules
dist
coverage
.nx
pnpm-lock.yaml
*.tsbuildinfo
.helm/templates/
deploy/argocd/application.yaml

libs/frontend/api-client/src/generated

# Generated contracts and tooling baselines are regenerated by scripts.
packages/tooling/baselines/**

# `nrb setup` owns the byte content of the selected-closure artifacts and records a SHA-256
# digest over them. Reformatting these invalidates that digest, so `nrb doctor` reports the
# closure as stale and `nrb closure install` then refuses to run.
.nrb/**
nrb.config.json

# Same ownership rule: `nrb doctor` byte-compares these against what setup would emit and
# reports any difference as capability drift, so a formatter must not rewrite them.
**/capabilities.generated.ts

# Script implementation package is checked by node --check and purpose-built QA commands.
packages/tooling/src/commands/**
playwright.extended.config.ts
stryker.config.mjs

test-results

playwright-report

.cache

# Local agent worktrees and archived implementation records are not canonical source.
.claude/worktrees/**
.superpowers/**
docs/superpowers/**

tmp

*.log

docs/command-matrix.md

docs/testing/modern-qa.md
