# @app/backend-feature-auth-admin

## Purpose

Owns privileged authentication analytics endpoints, retention orchestration, and admin-only login insight queries.

## Commands

```bash
pnpm exec nx run @app/backend-feature-auth-admin:test
pnpm exec nx run @app/backend-feature-auth-admin:build
```
