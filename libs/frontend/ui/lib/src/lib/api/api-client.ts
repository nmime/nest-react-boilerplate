/* v8 ignore file -- deprecated compatibility re-export for legacy deep imports only. */
/**
 * @deprecated Import API fetch helpers from `@app/frontend-api-support`.
 * This module is intentionally omitted from @app/frontend-ui's public barrel so
 * the shared UI package stays design-system-only for new consumers.
 */
export * from "@app/frontend-api-support";
