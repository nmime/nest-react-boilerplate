# user-app Instructions

Follow [frontend app rules](../AGENTS.md) and the root
[AGENTS.md](../../../AGENTS.md).

This app owns the current Vite user SPA shell. Keep shared user feature code,
API clients, runtime helpers, and UI in `libs/frontend/**`; do not collapse it
into `site-app` without explicit migration scope. See [README.md](README.md) for
commands and ownership.
