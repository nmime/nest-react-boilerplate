#!/usr/bin/env node
import { readdirSync, readFileSync } from "node:fs"; import { join } from "node:path";
const root = process.env.OPENAPI_CONTRACTS_ROOT ?? "contracts/openapi"; const methods = new Set(["get", "put", "post", "delete", "patch", "options", "head", "trace"]); const errors = [];
function walkRefs(value, visit) { if (!value || typeof value !== "object") return; if (typeof value.$ref === "string") visit(value.$ref); for (const item of Array.isArray(value) ? value : Object.values(value)) walkRefs(item, visit); }
function targetExists(doc, ref) { if (!ref.startsWith("#/")) return true; let cur = doc; for (const part of ref.slice(2).split("/").map((x) => x.replaceAll("~1", "/").replaceAll("~0", "~"))) cur = cur?.[part]; return cur !== undefined; }
for (const file of readdirSync(root).filter((name) => name.endsWith(".json")).sort()) { const path = join(root, file); let doc; try { doc = JSON.parse(readFileSync(path, "utf8")); } catch (error) { errors.push(`${path}: invalid JSON: ${error.message}`); continue; } if (!/^3\./.test(String(doc.openapi ?? ""))) errors.push(`${path}: openapi must be 3.x`); const operationIds = new Set(); for (const [route, item] of Object.entries(doc.paths ?? {})) for (const [method, operation] of Object.entries(item ?? {})) { if (!methods.has(method)) continue; const label = `${path} ${method.toUpperCase()} ${route}`; if (!operation.operationId) errors.push(`${label}: missing operationId`); else if (operationIds.has(operation.operationId)) errors.push(`${label}: duplicate operationId ${operation.operationId}`); else operationIds.add(operation.operationId); if (!Array.isArray(operation.tags) || operation.tags.length === 0) errors.push(`${label}: missing tags`); if (!operation.responses || Object.keys(operation.responses).length === 0) errors.push(`${label}: missing responses`); for (const [status, response] of Object.entries(operation.responses ?? {})) { if (!Object.hasOwn(response, "description")) errors.push(`${label}: response ${status} missing description property`); const content = response.content ?? {}; for (const [mediaType, media] of Object.entries(content)) if (mediaType.includes("json") && !media.schema) errors.push(`${label}: response ${status} ${mediaType} missing schema`); } for (const requirement of operation.security ?? []) for (const name of Object.keys(requirement)) if (!doc.components?.securitySchemes?.[name]) errors.push(`${label}: references undefined security scheme ${name}`); } walkRefs(doc, (ref) => { if (!targetExists(doc, ref)) errors.push(`${path}: unresolved $ref ${ref}`); }); }
if (errors.length) { console.error("OpenAPI lint failed:"); for (const error of errors) console.error(`- ${error}`); process.exit(1); } console.log(JSON.stringify({ status: "ok", contractsRoot: root }));
