import { createHash } from "node:crypto"; import { createReadStream, existsSync, mkdirSync, readFileSync, statSync, writeFileSync } from "node:fs"; import { createServer } from "node:http"; import { extname, join, normalize, relative, resolve, sep } from "node:path"; import { chromium } from "@playwright/test";
const storyIds = ["components-uibutton--primary", "components-uibutton--secondary-link"]; const baselinePath = resolve("packages/tooling/baselines/visual/storybook-ui.json"); const outputDir = resolve("test-results/storybook-visual"); const updateBaselines = process.env.UPDATE_VISUAL_BASELINES === "1"; const storybookRoot = resolve("dist/storybook/frontend-ui"); const providedUrl = process.env.STORYBOOK_URL;
const contentTypes = new Map([[".css", "text/css; charset=utf-8"], [".html", "text/html; charset=utf-8"], [".js", "text/javascript; charset=utf-8"], [".json", "application/json; charset=utf-8"], [".map", "application/json; charset=utf-8"], [".png", "image/png"], [".svg", "image/svg+xml"], [".txt", "text/plain; charset=utf-8"], [".woff", "font/woff"], [".woff2", "font/woff2"]]);
const trim = (value) => { let out = value; while (out.startsWith("/") || out.startsWith(String.fromCharCode(92))) out = out.slice(1); return out; }; const hashBuffer = (buffer) => createHash("sha256").update(buffer).digest("hex"); const readBaselines = () => existsSync(baselinePath) ? JSON.parse(readFileSync(baselinePath, "utf-8")) : {}; const isInsideRoot = (root, candidate) => { const rel = relative(root, candidate); return rel === "" || (!rel.startsWith("..") && !rel.includes(`..${sep}`)); };
async function createStaticServer(root) { if (!existsSync(root)) throw new Error(`Storybook build directory not found: ${root}`); const server = createServer((request, response) => { const pathname = decodeURIComponent(new URL(request.url ?? "/", "http://127.0.0.1").pathname); let filePath = join(root, trim(normalize(pathname)) || "index.html"); if (!isInsideRoot(root, filePath)) { response.writeHead(403).end("Forbidden"); return; } if (existsSync(filePath) && statSync(filePath).isDirectory()) filePath = join(filePath, "index.html"); if (!existsSync(filePath)) { response.writeHead(404).end("Not found"); return; } response.writeHead(200, { "content-type": contentTypes.get(extname(filePath)) ?? "application/octet-stream" }); createReadStream(filePath).pipe(response); }); await new Promise((resolveListen, rejectListen) => { server.once("error", rejectListen); server.listen(0, "127.0.0.1", resolveListen); }); const address = server.address(); if (!address || typeof address === "string") throw new Error("Unable to determine visual regression server address"); return { url: `http://127.0.0.1:${address.port}`, close: async () => new Promise((resolveClose) => server.close(() => resolveClose())) }; }
async function captureStory(page, baseUrl, storyId) { await page.goto(`${baseUrl}/iframe.html?id=${storyId}&viewMode=story`, { waitUntil: "networkidle" }); await page.locator("#storybook-root").waitFor({ state: "visible" }); return await page.screenshot({ fullPage: false }); }
let browser, staticServer, exitCode = 0; try { mkdirSync(outputDir, { recursive: true }); let baseUrl = providedUrl; if (!baseUrl) { staticServer = await createStaticServer(storybookRoot); baseUrl = staticServer.url; } browser = await chromium.launch(); const page = await browser.newPage({ viewport: { width: 1024, height: 768 }, deviceScaleFactor: 1, reducedMotion: "reduce" }); const baselines = readBaselines(); const nextBaselines = {}; const failures = []; for (const storyId of storyIds) { const screenshot = await captureStory(page, baseUrl, storyId); const actual = { sha256: hashBuffer(screenshot), byteLength: screenshot.byteLength }; nextBaselines[storyId] = actual; const baseline = baselines[storyId]; if (!updateBaselines && !baseline) { failures.push(`${storyId}: missing baseline`); writeFileSync(join(outputDir, `${storyId}.missing.png`), screenshot); continue; } if (!updateBaselines && (baseline.sha256 !== actual.sha256 || baseline.byteLength !== actual.byteLength)) { failures.push(`${storyId}: expected ${baseline.sha256}/${baseline.byteLength}, got ${actual.sha256}/${actual.byteLength}`); writeFileSync(join(outputDir, `${storyId}.mismatch.png`), screenshot); } } if (updateBaselines) { mkdirSync(resolve("packages/tooling/baselines/visual"), { recursive: true }); writeFileSync(baselinePath, `${JSON.stringify(nextBaselines, null, 2)}\n`); console.log(`Updated Storybook visual baselines at ${baselinePath}`); } else if (failures.length) { console.error(failures.join("\n")); exitCode = 1; } else console.log("Storybook visual baselines match."); } catch (error) { console.error(error); exitCode = 1; } finally { await browser?.close().catch(() => undefined); await staticServer?.close().catch(() => undefined); } process.exit(exitCode);
