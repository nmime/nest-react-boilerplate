{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "$id": "@repo/tooling/feature",
  "title": "Feature",
  "description": "Generate a vertical-slice feature across backend and frontend layers",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Feature name (kebab-case or multi-word; auto-converted to kebab-case)",
      "x-prompt": "What name would you like to give your feature?",
      "x-priority": "important"
    },
    "apiApp": {
      "type": "string",
      "description": "Backend API application that owns this feature",
      "x-prompt": "Which backend API application owns this feature?",
      "x-priority": "important"
    },
    "frontendApp": {
      "type": "string",
      "description": "Frontend application that hosts the feature page",
      "x-prompt": "Which frontend application hosts this feature?",
      "x-priority": "important"
    },
    "migrationTimestamp": {
      "oneOf": [
        { "type": "string", "pattern": "^[0-9]{14}$" },
        { "type": "integer", "minimum": 10000000000000, "maximum": 99999999999999 }
      ],
      "description": "Optional deterministic migration timestamp in YYYYMMDDHHmmss form",
      "x-priority": "optional"
    },
    "dryRun": {
      "type": "boolean",
      "description": "Show what would be generated without writing files",
      "default": false,
      "x-priority": "optional"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting the changed files",
      "default": false,
      "x-priority": "optional"
    }
  },
  "required": ["name", "apiApp", "frontendApp"]
}
