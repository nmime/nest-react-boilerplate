# Starter App

Generated Vite/React frontend application at `apps/frontend/starter-app`.

## Verification

```bash
pnpm exec nx run starter-app:build
pnpm exec nx run starter-app:test
pnpm run frontend:fsd:check
```

Nx tags: `platform:frontend`, `type:frontend-app`, `scope:starter`, `fsd:layer:app`.
