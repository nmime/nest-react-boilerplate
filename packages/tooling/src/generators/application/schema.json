{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "$id": "@repo/tooling/application",
  "title": "Application",
  "description": "Generate a new application following repository conventions",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Application name (kebab-case)",
      "x-prompt": "What name would you like to give your application?",
      "x-priority": "important"
    },
    "kind": {
      "type": "string",
      "description": "Application kind",
      "enum": ["frontend", "backend"],
      "x-prompt": "What kind of application?",
      "x-priority": "important"
    },
    "directory": {
      "type": "string",
      "description": "Directory path prefix (default: apps/<kind>/<name>)",
      "x-priority": "optional"
    },
    "tags": {
      "type": "string",
      "description": "Comma-separated list of Nx tags (auto-generated by default)",
      "x-priority": "optional"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting the changed files",
      "default": false,
      "x-priority": "optional"
    }
  },
  "required": ["name", "kind"]
}
