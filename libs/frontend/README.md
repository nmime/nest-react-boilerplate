# Frontend Libraries

Frontend libraries live under `libs/frontend/**/lib`. They provide web UI, native UI, runtime helpers, API clients/support, and feature-shared frontend code for deployables in `apps/frontend/**`.

Use [AGENTS.md](AGENTS.md) for frontend-library rules and [Frontend FSD](../../docs/frontend-fsd.md) for import boundaries.
