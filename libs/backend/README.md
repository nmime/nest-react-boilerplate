# Backend Libraries

Backend libraries live under `libs/backend/**/lib`. They provide backend common foundations, feature modules, test utilities, and PostgreSQL/data-access projects for backend deployables in `apps/backend/**`.

Use [AGENTS.md](AGENTS.md) for backend-library rules and [Architecture](../../docs/architecture.md) for the full backend split.
