// Test-only library: hosts the auth feature's Postgres/Testcontainers
// component specs (run via the `component-test` target). It exposes no public
// runtime API; consumers import the auth feature through
// @app/backend-feature-auth-main and @app/backend-postgres-main-auth.
export {};
