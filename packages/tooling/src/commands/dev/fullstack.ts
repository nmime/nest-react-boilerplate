#!/usr/bin/env node
// @ts-nocheck
import { spawn } from "node:child_process";
const env = { ...process.env, AUTH_PERSISTENCE: process.env.AUTH_PERSISTENCE ?? "postgres", AUTH_JWT_SECRET: process.env.AUTH_JWT_SECRET ?? "local-dev-jwt-secret-change-me", AUTH_JWT_ISSUER: process.env.AUTH_JWT_ISSUER ?? "nest-react-boilerplate-local", AUTH_JWT_AUDIENCE: process.env.AUTH_JWT_AUDIENCE ?? "nest-react-boilerplate-api", DATABASE_URL: process.env.DATABASE_URL ?? "postgres://postgres:postgres@localhost:5432/nest_react_boilerplate", VITE_AUTH_API_BASE_URL: process.env.VITE_AUTH_API_BASE_URL ?? "http://localhost:3003", VITE_USER_API_BASE_URL: process.env.VITE_USER_API_BASE_URL ?? "http://localhost:3002", VITE_ADMIN_API_BASE_URL: process.env.VITE_ADMIN_API_BASE_URL ?? "http://localhost:3001" };
const run = (command, args, options = {}) => new Promise((resolve, reject) => { const child = spawn(command, args, { stdio: "inherit", shell: process.platform === "win32", env, ...options }); child.on("exit", (code) => code === 0 ? resolve() : reject(new Error(`${command} ${args.join(" ")} exited with ${code}`))); });
await run("docker", ["compose", "up", "-d", "postgres"]); await run("node", ["packages/tooling/bin/repo-tooling.mjs", "db", "migrate"]); console.log("Starting APIs and frontends. URLs: admin http://localhost:4200, user http://localhost:4201, landing http://localhost:4202, APIs 3001/3002/3003."); await run("pnpm", ["exec", "nx", "run-many", "-t", "serve", "--projects=backend-admin-app-api,user-app-api,auth-app-api,admin-app,user-app,landing-app", "--parallel=6"]);
