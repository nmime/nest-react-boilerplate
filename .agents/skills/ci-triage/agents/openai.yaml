interface:
  display_name: 'Triage CI'
  short_description: 'Diagnose repository CI and pipeline failures'
  default_prompt: 'Use $ci-triage to find the first real failure and verify the smallest safe fix.'
