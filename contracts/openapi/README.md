# Generated OpenAPI contracts

The JSON files in this directory are generated, committed API contracts.

Regenerate them with:

```bash
pnpm api:contracts
```

These files are inputs for API contract checks and typed frontend client generation. They are not hand-authored human documentation; update controllers, DTOs, Swagger metadata, or generation tooling and then regenerate.
