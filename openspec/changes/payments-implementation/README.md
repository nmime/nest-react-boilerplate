# payments-implementation

Add provider-agnostic payment ordering, eight verified provider adapters behind
one port, receipt-first webhook ingress with idempotent redelivery, and an
admin provider lifecycle to the selected backends, on the postgres persistence
axis (mongodb ships as an inert reference axis).
